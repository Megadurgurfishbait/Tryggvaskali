import React from 'react';
import styled from 'styled-components';
import Layout from '../../layout/';
import { Colors } from '../../Assets';

const Menu = () => {
    return (
        <Layout bgColor={Colors.GREEN}>Hæ</Layout>
    )
}

export default Menu;