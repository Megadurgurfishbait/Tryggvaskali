import React from "react";
import styled from "styled-components";
import Layout from "../../layout/";
import { Colors } from "../../Assets";

const About = () => {
  return <Layout bgColor={Colors.LIGHT_GREEN}>Hæ</Layout>;
};

export default About;
